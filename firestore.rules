rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access is handled server-side via Firebase Admin SDK (Server Actions).
    // No client-side Firestore reads/writes are permitted.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
