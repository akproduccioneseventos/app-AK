const handleAddVideo = async () => {
    // Your existing video add logic goes here
    // Ensure proper scoping and no top-level await
    // Add validations and setIsSavingVideo logic as needed
};

// Existing video add logic starts here
// Ensure to call handleAddVideo() where appropriate