import type { ReactNode } from 'react';
import { AkRedPremiumSurface } from '@/components/brand/ak-red-premium-surface';

export default function EventoSocialLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
