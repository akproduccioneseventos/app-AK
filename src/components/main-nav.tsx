// src/components/main-nav.tsx
'use client';

// This component is no longer used as the sidebar has been removed.
// Keeping the file with a null-rendering component to avoid breaking imports immediately.
// Consider deleting this file if no other part of the application imports it.
export function MainNav() {
  return null;
}

    